---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.10.0
  creationTimestamp: null
  name: connections.guacamole-operator.github.io
spec:
  group: guacamole-operator.github.io
  names:
    kind: Connection
    listKind: ConnectionList
    plural: connections
    singular: connection
  scope: Namespaced
  versions:
  - name: v1alpha1
    schema:
      openAPIV3Schema:
        description: Connection is the Schema for the connections API.
        properties:
          apiVersion:
            description: 'APIVersion defines the versioned schema of this representation
              of an object. Servers should convert recognized schemas to the latest
              internal value, and may reject unrecognized values. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources'
            type: string
          kind:
            description: 'Kind is a string value representing the REST resource this
              object represents. Servers may infer this from the endpoint the client
              submits requests to. Cannot be updated. In CamelCase. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds'
            type: string
          metadata:
            type: object
          spec:
            description: ConnectionSpec defines the desired state of Connection.
            properties:
              parent:
                default: /
                description: Parent of the connection specified as a path (/<group>/<group>).
                  Defaults to ROOT if not specified.
                type: string
              protocol:
                description: Protocol of the connection.
                type: string
            type: object
          status:
            description: ConnectionStatus defines the observed state of Connection.
            properties:
              identifier:
                description: Guacamole internal identifier of the connection. Missing
                  if connection not yet configured.
                type: string
              parent:
                description: Guacamole internal identifier of the connection's parent
                  group. Missing if connection not yet configured.
                type: string
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
